[project]
name = "skill-creator"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic",
    "pyyaml",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
